div Data Upload
  div(class="form-group" style="width: 20%; margin: 0px auto;")
    form(id="fileForm" method="post" action="/uploadFile" enctype="multipart/form-data")
      label(for="passwordInput") File to Upload
      input(type="file" class="form-control-file" id="inputFile" aria-describedby="fileHelp")
      small(id="fileHelp" class="form-text text-muted") GEDCOM files are supported
      input(type="submit" class="btn btn-priary" id="submitButton")
